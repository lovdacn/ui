---
title: Checkbox
description: A control that allows the user to toggle between checked and not checked.
component: true
---

<ComponentPreviewCard title="Checkbox" />

## Installation

<PackageManagerTabs>
<PMTabContent value="npm">

```bash
npx lovdacn@latest add checkbox
```

</PMTabContent>
<PMTabContent value="yarn">

```bash
yarn dlx lovdacn@latest add checkbox
```

</PMTabContent>
<PMTabContent value="pnpm">

```bash
pnpm dlx lovdacn@latest add checkbox
```

</PMTabContent>
<PMTabContent value="bun">

```bash
bunx --bun lovdacn@latest add checkbox
```

</PMTabContent>
</PackageManagerTabs>

*(You can also use the shortened `lvcn` alias, e.g., `npx lvcn@latest add checkbox`)*

This copies the component source into your project (typically `components/ui/checkbox.tsx`) and installs any required dependencies.

**Registry dependencies:** `utils`

## Usage

```tsx
import { Checkbox } from "@/components/ui/checkbox"
import { Label } from "@/components/ui/label"
import { Text } from "@/components/ui/text"
import { View } from "react-native"

export function Example() {
  return (
    <View className="flex-row items-center gap-2">
      <Checkbox id="terms" />
      <Label htmlFor="terms">
        <Text>Accept terms and conditions</Text>
      </Label>
    </View>
  )
}
```

## Styles

lvcn components are available across all style packs:

| Style | Notes |
| --- | --- |
| `default` | Classic rounded system |
| `new-york` | Compact, refined |
| `nova` | Sharp corners (default in many setups) |
| `luma` / `lyra` / `maia` / `mira` | Radius & font variants |
| `rhea` / `sera` / `vega` | Pill, zero-radius, balanced |

Style is set at project init via `lvcn.json` (`style` + `styleEngine`).

## Platform

These components target **Expo / React Native** with **NativeWind** or **Uniwind**. They use `Platform.select` where web and native need different behavior.
