---
title: Progress
description: Displays an indicator showing the completion progress of a task.
component: true
---

<ComponentPreviewCard title="Progress" />

## Installation

<PackageManagerTabs>
<PMTabContent value="npm">

```bash
npx lovdacn@latest add progress
```

</PMTabContent>
<PMTabContent value="yarn">

```bash
yarn dlx lovdacn@latest add progress
```

</PMTabContent>
<PMTabContent value="pnpm">

```bash
pnpm dlx lovdacn@latest add progress
```

</PMTabContent>
<PMTabContent value="bun">

```bash
bunx lovdacn@latest add progress
```

</PMTabContent>
</PackageManagerTabs>

*(You can also use the shortened `lvcn` alias, e.g., `npx lvcn@latest add progress`)*

This copies the component source into your project (typically `components/ui/progress.tsx`) and installs any required dependencies.

**Registry dependencies:** `utils`

## Usage

```tsx
import { Progress } from "@/components/ui/progress"

export function Example() {
  return <Progress value={33} className="w-full" />
}
```

## Styles

lvcn components are available across all style packs:

| Style | Notes |
| --- | --- |
| `default` | Classic rounded system |
| `new-york` | Compact, refined |
| `nova` | Sharp corners (default in many setups) |
| `luma` / `lyra` / `maia` / `mira` | Radius & font variants |
| `rhea` / `sera` / `vega` | Pill, zero-radius, balanced |

Style is set at project init via `lvcn.json` (`style` + `styleEngine`).

## Platform

These components target **Expo / React Native** with **NativeWind** or **Uniwind**. They use `Platform.select` where web and native need different behavior.
