---
title: Label
description: Renders an accessible label associated with controls.
component: true
---

<ComponentPreviewCard title="Label" />

## Installation

<PackageManagerTabs>
<PMTabContent value="npm">

```bash
npx lovdacn@latest add label
```

</PMTabContent>
<PMTabContent value="yarn">

```bash
yarn dlx lovdacn@latest add label
```

</PMTabContent>
<PMTabContent value="pnpm">

```bash
pnpm dlx lovdacn@latest add label
```

</PMTabContent>
<PMTabContent value="bun">

```bash
bunx --bun lovdacn@latest add label
```

</PMTabContent>
</PackageManagerTabs>

*(You can also use the shortened `lvcn` alias, e.g., `npx lvcn@latest add label`)*

This copies the component source into your project (typically `components/ui/label.tsx`) and installs any required dependencies.

**Registry dependencies:** `utils`, `text`

## Usage

```tsx
import { Label } from "@/components/ui/label"
import { Text } from "@/components/ui/text"

export function Example() {
  return (
    <Label>
      <Text>Email</Text>
    </Label>
  )
}
```

## Styles

lvcn components are available across all style packs:

| Style | Notes |
| --- | --- |
| `default` | Classic rounded system |
| `new-york` | Compact, refined |
| `nova` | Sharp corners (default in many setups) |
| `luma` / `lyra` / `maia` / `mira` | Radius & font variants |
| `rhea` / `sera` / `vega` | Pill, zero-radius, balanced |

Style is set at project init via `lvcn.json` (`style` + `styleEngine`).

## Platform

These components target **Expo / React Native** with **NativeWind** or **Uniwind**. They use `Platform.select` where web and native need different behavior.
